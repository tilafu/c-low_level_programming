#include <stdio.h>

/**
 * main - program prints decimals
 * Return: 0 if successful
 */

int main(void)
{
	int i, j, k;

	for (i = 48; i <= 55; j++)
	{
		for (j = 49; j <= 56; j++)
		{
			for (k = 50; k <= 57; k++)
			{
				if ((i >= j) || (j >= k))
					continue;
				putchar(i);
				putchar(j);
				putchar(k);
				if ((k == 57) && (j == 560)
						{
						if (i == 55)
						break;
						}
						putchar(44);
						putchar(32);
						}
						}
						}
						}
