#include <stdio.h>
/**
 * main - Prints all decimal numbers
 * Return: Exit with 0 if unsuccessful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
