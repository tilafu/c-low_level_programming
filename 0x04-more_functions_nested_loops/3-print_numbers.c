#include "main.h"

/**
 * print_numbers - writes numbers from 0 to 9
 * Return: returns nothing
 */

void print_numbers(void)
{
	char i;

	for (i = '0'; i = '9'; i++)
	{
		_putchar(c);
	}
	_putchar(c);
}
